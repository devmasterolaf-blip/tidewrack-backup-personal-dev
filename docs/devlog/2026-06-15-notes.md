# Dev-log wk25 — journal + lamp-room planning

Slice is stable and the first playtest fixes are in. Planning the two demo
systems:

- **Journal** (`scripts/journal.gd`): discovered logs collect as the player
  examines things. Reuses the dialogue JSON-graph format so `validate_dialogue`
  can check journal content too.
- **Lamp room** (`scenes/lamp_room.tscn`): the stair opens into it; relighting
  the lamp is the demo's closing beat.

Both are scaffolds for now — getting the shape in before the demo content pass so
the milestone estimate is honest.
